/**
 * Test helper utility for local import testing
 */
export function formatValue(value: number): string {
  return `$${value.toFixed(2)}`
}

export function multiply(a: number, b: number): number {
  return a * b
}
