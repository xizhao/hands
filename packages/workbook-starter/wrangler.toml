name = "workbook-starter"
main = "src/index.tsx"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "development"
# PostgreSQL connection provided by Hands runtime
DATABASE_URL = ""

# Cron jobs - add scheduled tasks here
# [triggers]
# crons = ["0 * * * *"]  # Example: every hour

[dev]
port = 8787
local_protocol = "http"
