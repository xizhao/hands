name = "{{name}}"
main = "src/index.tsx"
compatibility_date = "2024-01-01"

[vars]
ENVIRONMENT = "development"
# PostgreSQL connection provided by Hands desktop app
DATABASE_URL = "{{database_url}}"

# Cron jobs - add scheduled tasks here
# [triggers]
# crons = ["0 * * * *"]  # Example: every hour

[dev]
port = 8787
local_protocol = "http"
