# Cloudflare Pages configuration
name = "hands-site"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"

# R2 bucket for hosting release artifacts (DMG, etc)
[[r2_buckets]]
binding = "RELEASES"
bucket_name = "hands-releases"

[vars]
# Set via dashboard or wrangler secret
# OPENROUTER_API_KEY = ""
